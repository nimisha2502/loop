#include <stdio.h>

int main()
{
    int num,i,rem,sum=0;
    printf("\n enter the no.: ");      
    scanf("%d",&num);
    printf("\n input no.:%d",num);
    for(i=num;i!=0;i++)
    {
      rem=num%10;
      sum+=rem;
      num=num/10;
    }
    printf("\n sum of digits=%d",sum);
    return 0;
}
